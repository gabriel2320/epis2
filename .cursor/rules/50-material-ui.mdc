---
description: Material UI único — sin Carbon, español, no dashboard home
globs: "{apps/web/**,packages/design-system/**}"
alwaysApply: false
---

# Material UI (único frontend visible)

- Solo MUI en superficie clínica; **rechazar** Carbon / Soft Carbon aunque existan en EPIS.
- Tema en `packages/design-system`.
- Microcopy visible en **español** (`spanish-visible-copy` gate).
- Home futura = Command Center; sin layout dashboard.
- Una acción principal por vista.
