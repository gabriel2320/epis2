# EPIS2 — Velocidad dev

Sigue `docs/dev/EPIS2_DEV_VELOCITY.md` y la skill `epis2-velocity`:

1. `npm run dev:velocity` (añade `--refresh` si el brief es viejo)
2. Trabaja un solo objetivo con alcance SDEPIS2 declarado
3. `npm run dev:velocity:gates` al cerrar (gates del rol, no todos los quality:*)
