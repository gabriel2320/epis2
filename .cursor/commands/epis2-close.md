# EPIS2 — Cierre con gates

Sigue la skill `epis2-close`:

1. Ejecuta `npm run check`, `npm run test`, `npm run db:validate`
2. Escribe reporte en `reports/` con alcance, gates, riesgos y próximo paso
3. Detente si `architecture:validate` falla o hay contradicción con invariantes
