/** Subpath `@epis2/epis2-ui/data` — core síncrono (catálogo dev / tests). Producción usa `EpisDataGridSuspense`. */
export {
  EpisDataGrid,
  type EpisDataGridProps,
  type EpisDataGridRow,
  type GridColDef,
  type GridRowParams,
} from './EpisDataGridCore.js';
