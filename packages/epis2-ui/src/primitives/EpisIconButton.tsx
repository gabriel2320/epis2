import MuiIconButton, { type IconButtonProps } from '@mui/material/IconButton';

export type EpisIconButtonProps = IconButtonProps;

export function EpisIconButton(props: EpisIconButtonProps) {
  return <MuiIconButton {...props} />;
}
