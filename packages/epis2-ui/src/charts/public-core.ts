/** Subpath `@epis2/epis2-ui/charts` — core síncrono (catálogo dev / tests). Producción usa `EpisTrendChartSuspense`. */
export {
  EpisTrendChart,
  type EpisTrendChartProps,
  type EpisTrendSeries,
} from './EpisTrendChartCore.js';
