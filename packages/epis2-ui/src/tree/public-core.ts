/** Subpath `@epis2/epis2-ui/tree` — core síncrono (catálogo dev / tests). Producción usa `EpisTreeViewSuspense`. */
export { EpisTreeView, type EpisTreeNode, type EpisTreeViewProps } from './EpisTreeViewCore.js';
