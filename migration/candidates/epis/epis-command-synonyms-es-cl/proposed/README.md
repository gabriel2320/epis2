# Adaptación prevista

Mapear `intentId` legacy (`clinical.evolution-note`, etc.) al catálogo EPIS2
(`create_evolution_draft`, `summarize_patient`, …) en `packages/command-registry`.

No importar el JSON sin tabla de equivalencia revisada.
