# S2 — Cierre F2-A/B (verificación PROG-DI)

**Fecha:** 2026-06-14 · **Alcance:** gates PROG-DI + E2E DI  
**Plan:** [`epis2-plan-conciliacion-triada-2026-06-14.md`](./epis2-plan-conciliacion-triada-2026-06-14.md)

---

## Gates principales

| Gate | Resultado |
|------|-----------|
| `npm run stack:dev` | ✓ |
| `npm run check` | ✓ |
| `npm run test` (1023) | ✓ |
| `npm run db:validate` | ✓ |
| `quality:di-*-gate` (9) + `quality:di-signoff-gate` | ✓ |

---

## E2E DI

```bash
VITE_ENABLE_DUAL_CHART_MODES=true npx playwright test \
  e2e/di-clinical-secretary-journey.spec.ts e2e/dual-chart-modes.spec.ts
```

| Resultado | Detalle |
|-----------|---------|
| **20/20 passed** | Tras fix hooks + helpers E2E |

---

## Fixes F2-B (esta sesión)

### Bug producto — hooks React

**Causa:** `ChartEspacioCommandDock` hacía `return null` antes de `useCommandResolveContext` / `useClinicalCommandSubmit`. Al pinchar paciente en `/espacio/receta`, segundo render añadía hooks → error boundary.

**Fix:** [`apps/web/src/components/chart/ChartEspacioCommandDock.tsx`](../../apps/web/src/components/chart/ChartEspacioCommandDock.tsx) — hooks siempre; `return null` después.

**Preventivo:** [`EpisDesignModeOverlay`](../../apps/web/src/design/EpisDesignModeProvider.tsx) — `useEpisSession()` antes del early return.

### E2E helpers

| Fix | Archivo |
|-----|---------|
| Paleta Ctrl+K input MUI | `fillCommandPaletteQuery` |
| Receta campos required + hydration | `fillMinimalPrescriptionDraft` |
| Timeline g5 bucket `last3Months` | `dual-chart-modes.spec.ts` |
| pinDemoCase dual chart | `demoPatient.ts` |
| Secretary queries + `window.confirm` | `di-clinical-secretary-journey.spec.ts` |

### Browser bundle (S2 previo)

`@epis2/test-fixtures` — sin `node:crypto` en barrel web; export `@epis2/test-fixtures/node`.

---

## Veredicto

| Criterio | Estado |
|----------|--------|
| Gates DI + check + test + db | ✓ |
| E2E secretary + dual-chart | ✓ **20/20** |
| Commit PROG-DI | ☐ Pendiente aprobación usuario |

**Siguiente (F2 commit):** commit único WIP PROG-DI + fixes; quitar `gitPending` en `di-ledger.json`; push `master`.

---

*Los errores de EPIS no son recuerdos: son gates de EPIS2.*
