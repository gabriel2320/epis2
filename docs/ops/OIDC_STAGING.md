# OIDC staging (MF-175)

## Modo actual

- `AUTH_MODE=hybrid` en staging: login demo + service key; OIDC real pendiente ADR-006.
- No se habilita auto-provisión ni bypass de aprobación clínica.

## Próximo paso

Integrar proveedor OIDC en staging con checklist de seguridad antes de piloto formal (MF-179).
