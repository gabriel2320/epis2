{
  "name": "required",
  "description": "Gate PR / pre-release — core clínico + regresión ficha-first (PROG-CONSOLIDATE Fase 1).",
  "steps": [
    "npm run check",
    "npm run test",
    "npm run db:validate",
    "npm run quality:gate -- quality:ficha-first-gate",
    "npx prettier --check ."
  ]
}
