# Design agents (lab)

Ollama-backed UI critics for **EpisDesignMode** only. Off by default.

Enable: `EPIS2_DESIGN_AGENTS_ENABLED=true` (never in piloto clínico).

**Ubicación:** `apps/web/src/lab/design-agents/` (MF-PURGE-09).

**No importar** desde shell clínico productivo (`pages/`, `components/`, `layouts/`, `cica/`, `routes/`). Permitido: `design/EpisDesignModeProvider`, `pages/dev/`, tests, `lab/`.
