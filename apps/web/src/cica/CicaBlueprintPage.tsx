import { CicaGeneratedScreen, type CicaGeneratedScreenProps } from '@epis2/epis2-ui';

/** Wrapper web — blueprint + slots (generación automatizada de pantallas). */
export function CicaBlueprintPage(props: CicaGeneratedScreenProps) {
  return <CicaGeneratedScreen {...props} />;
}
