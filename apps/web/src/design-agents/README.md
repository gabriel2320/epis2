# Design agents (lab)

Ollama-backed UI critics for **EpisDesignMode** only. Off by default.

Enable: `EPIS2_DESIGN_AGENTS_ENABLED=true` (never in piloto clínico).

**No importar** desde páginas `/espacio/*` productivas. Candidato a mover a `apps/web/src/lab/` (Tramo 2 — ver `reports/epis2-frontend-purge-cica-reform-plan.md`).
